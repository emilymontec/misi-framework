<?php

declare(strict_types=1);

namespace App\Services;

/**
 * {{class}}: lógica de aplicación/negocio.
 *
 * Los controllers deben delegar aquí en vez de acumular lógica compleja
 * (ver docs/architecture.md, sección "Responsabilidad de cada capa").
 * Inyecta el Repository que necesites por constructor, por ejemplo:
 *
 *   public function __construct(private readonly ExampleRepository $repository) {}
 */
final class {{class}}
{
    public function __construct()
    {
    }
}
