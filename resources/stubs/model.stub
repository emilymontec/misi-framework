<?php

declare(strict_types=1);

namespace App\Models;

use DateTimeImmutable;

/**
 * {{class}}: representa una fila de la tabla `{{table}}`.
 *
 * Misi no tiene ORM (ver docs/architecture.md) — este es un objeto de
 * datos simple (DTO), sin lógica de consultas ni de negocio. El acceso a
 * datos vive en un Repository (`php bin/biz make:repository`); la lógica
 * de aplicación, en un Service (`php bin/biz make:service`).
 *
 * Ajusta las propiedades para que coincidan con las columnas reales de
 * `{{table}}` — esto es solo un punto de partida.
 */
final class {{class}}
{
    public function __construct(
        public readonly int $id,
        public readonly DateTimeImmutable $createdAt,
    ) {
    }

    /** @param array<string, mixed> $row */
    public static function fromArray(array $row): self
    {
        return new self(
            id: (int) $row['id'],
            createdAt: new DateTimeImmutable((string) $row['created_at']),
        );
    }
}
